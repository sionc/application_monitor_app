<%= form_for(@event_log_entry) do |f| %>
  <% if @event_log_entry.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event_log_entry.errors.count, "error") %> prohibited this event_log_entry from being saved:</h2>

      <ul>
      <% @event_log_entry.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :message %><br />
    <%= f.text_area :message %>
  </div>
  <div class="field">
    <%= f.label :timestamp %><br />
    <%= f.datetime_select :timestamp %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
